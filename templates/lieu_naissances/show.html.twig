{% extends 'base.html.twig' %}

{% block title %}LieuNaissances{% endblock %}

{% block body %}
    <h1>LieuNaissances</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ lieu_naissance.id }}</td>
            </tr>
            <tr>
                <th>Designation</th>
                <td>{{ lieu_naissance.designation }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_lieu_naissances_index') }}">back to list</a>

    <a href="{{ path('app_lieu_naissances_edit', {'id': lieu_naissance.id}) }}">edit</a>

    {{ include('lieu_naissances/_delete_form.html.twig') }}
{% endblock %}
