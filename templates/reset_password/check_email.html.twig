{% extends 'base.html.twig' %}

{% block title %}E-mail de réinitialisation envoyé{% endblock %}

{% block body %}
    <div class="container mt-5">
        <h1 class="mb-4">E-mail envoyé !</h1>
        <p>
            Si un compte correspondant à cette adresse e-mail existe, un e-mail contenant un lien de réinitialisation vient d'être envoyé.
        </p>
        <p>
            Ce lien expirera dans 
            <strong>{{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}</strong>.
        </p>
        <p class="mt-3">
            Si vous ne recevez pas d'e-mail, vérifiez vos spams ou 
            <a href="{{ path('app_forgot_password_request') }}" class="text-decoration-none">essayez à nouveau</a>.
        </p>
        <p>
            <a href="{{ path('app_login') }}" class="btn btn-primary mt-2">Retour à la connexion</a>
        </p>
    </div>
{% endblock %}
