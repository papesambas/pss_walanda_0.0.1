{% extends 'base.html.twig' %}

{% block title %}Peres{% endblock %}

{% block body %}
    <h1>Peres</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pere.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_peres_index') }}">back to list</a>

    <a href="{{ path('app_peres_edit', {'id': pere.id}) }}">edit</a>

    {{ include('peres/_delete_form.html.twig') }}
{% endblock %}
